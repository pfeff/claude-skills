# Node: ${NODE_ID} - ${HEADLINE}

## Workspace

- **Path**: ${WORKSPACE_PATH}
- **Project**: ${PROJECT_DIR}
- **Branch**: ${NODE_BRANCH}

## Repos

${REPOS_LIST}

## Context

- **Project**: ${PROJECT_SLUG}
- **Integration branch**: ${PROJECT_BRANCH}
- **Node branch**: ${NODE_BRANCH}

## Instructions

- See **DESIGN.md** for requirements and acceptance criteria.
- Use `TaskList` for implementation progress; `cd ${PROJECT_DIR}` and `Read(${PROJECT_DIR}/GOAL.md)` for project state.
- This workspace auto-advances (implement → validate → commit → next); see the `auto-advance` operation for loop/stop behavior.
