# Node: ${NODE_ID} - ${HEADLINE}

## Workspace

- **Path**: ${WORKSPACE_PATH}
- **Project**: ${PROJECT_DIR}
- **Branch**: ${NODE_BRANCH}

## Repos

${REPOS_LIST}

## Context

- **Project**: ${PROJECT_SLUG}
- **Integration branch**: ${PROJECT_BRANCH}
- **Node branch**: ${NODE_BRANCH}

## Instructions

- See **DESIGN.md** for requirements and acceptance criteria.
- Use `TaskList` for implementation progress; `cd ${PROJECT_DIR}` and `Read(${PROJECT_DIR}/GOAL.md)` for project state.
- This workspace auto-advances (implement → validate → commit → next); see the `auto-advance` operation for loop/stop behavior.
- **This node is working space, not storage.** Close it via the sanctioned close path, never bare `git worktree remove`/`rm -rf`. And regardless of how it closes: any spec, design doc, or decision record meant to outlive this node must go to the Obsidian vault (via `obsidian-notes`) during the work — not stay only in this node's DESIGN.md.
