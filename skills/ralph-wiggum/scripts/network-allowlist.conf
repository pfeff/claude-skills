# Network allowlist for Ralph Wiggum sandbox container
# One domain per line. Lines starting with # are comments.
# DNS resolution happens at container start time.

# Claude API (required)
api.anthropic.com

# Git operations
github.com
api.github.com

# Package registries
registry.npmjs.org
pypi.org
files.pythonhosted.org
