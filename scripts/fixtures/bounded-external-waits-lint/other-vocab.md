# other-vocab fixture

Fixture (g): the remaining closed-vocabulary terms (wget, ssh, osascript),
each unwrapped and external. All three must be flagged.

```bash
wget -q https://releases.example.com/latest.tar.gz
ssh deploy@build.example.com 'run-release'
osascript -e 'tell application "Reminders" to get every reminder'
```
